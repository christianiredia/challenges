let value = 430;
let tip = value >= 50 && value <= 300 ? value * 0.15 : value * 0.2;
let totalValue = value + tip;

console.log(
  `The Bill was $${value}, the tip was $${tip}, and the total value as ${totalValue}`
);

console.log(
  `The Bill was $${value}, the tip was $${tip}, and the total value as ${totalValue}`
);

console.log(
  `The Bill was $${value}, the tip was $${tip}, and the total value as ${totalValue}`
);
